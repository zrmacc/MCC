% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/PseudoRegression.R
\name{PseudoReg}
\alias{PseudoReg}
\title{Pseudo-Value Regression with Second-Order-Corrected Inference}
\usage{
PseudoReg(
  formula,
  data,
  tau,
  type = c("MCF", "AUC"),
  covariates = NULL,
  cens_after_last = TRUE,
  idx_name = "idx",
  status_name = "status",
  time_name = "time",
  jump_weights = NULL,
  conf_level = 0.95
)
}
\arguments{
\item{formula}{One-sided regression formula, for example
\code{~ arm + age + arm:age}.}

\item{data}{Long-format recurrent-event data.}

\item{tau}{Positive truncation time within the observed time range.}

\item{type}{\code{"MCF"} for the MCF at \code{tau}, or \code{"AUC"} for
the area under the MCF through \code{tau}.}

\item{covariates}{Optional subject-level data frame containing the subject
identifier and variables used by \code{formula}. If omitted, these
variables are extracted from \code{data} and must be constant by subject.}

\item{cens_after_last}{Should subjects without an explicit terminal row be
censored after their last observed event?}

\item{idx_name}{Name of the subject identifier column.}

\item{status_name}{Name of the status column: 0 censoring, 1 recurrent event,
and 2 death.}

\item{time_name}{Name of the observation-time column.}

\item{jump_weights}{Optional finite recurrent-event jump weights, either one
value or one value per row of \code{data}.}

\item{conf_level}{Confidence level for normal Wald intervals.}
}
\value{
An object of class \code{PseudoReg}. Its \code{vcov} component and
  associated methods use the second-order-corrected covariance. The ordinary
  \code{lm} fit is available in \code{fit}.
}
\description{
Fits an identity-link ordinary least-squares regression to MCF or AUMCF
pseudo-values and estimates coefficient covariance using the full
first-order Hajek projection, including the second-order contribution.
}
\details{
The covariance uses the empirical first-order Hajek projection
  \code{h11 + h12} with HC0 normalization and normal Wald inference. This
  procedure assumes completely independent censoring and the regularity
  conditions required for the MCF or AUMCF functional expansion.
}
\examples{
set.seed(10)
design <- cbind(intercept = 1, arm = rep(0:1, each = 10))
event_data <- GenData(covariates = design, beta_event = c(0, log(0.8)),
                      beta_death = c(0, 0), n = 20, tau = 3)
fit <- PseudoReg(~ arm, event_data, tau = 2, type = "MCF")
coef(fit)
vcov(fit)
}
