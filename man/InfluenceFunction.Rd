% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/InfluenceFunction.R
\name{InfluenceFunction}
\alias{InfluenceFunction}
\title{Calculate Influence Function at Truncation Time}
\usage{
InfluenceFunction(
  data,
  tau,
  type = c("MCF", "AUC"),
  mcf = NULL,
  weights = NULL
)
}
\arguments{
\item{data}{Data.frame with columns \code{idx}, \code{status}, \code{time}.}

\item{tau}{Truncation time.}

\item{type}{Character. \code{"MCF"} for the MCF influence at \code{tau}, or
\code{"AUC"} for the AUC influence (area under the MCF up to \code{tau}).}

\item{mcf}{Tabulated MCF as returned by \code{\link{CalcMCF}}. If \code{NULL},
computed from \code{data}.}

\item{weights}{Optional numeric vector of weights (one per row of \code{data}).}
}
\value{
Data.frame with columns \code{idx}, \code{psi} (one row per subject).
}
\description{
Returns the influence function contributions (idx, psi) at the truncation
time \code{tau}. One row per subject. The choice of \code{type} determines
whether the influence is for the mean cumulative function (MCF) at \code{tau}
or for the area under the MCF up to \code{tau} (AUC).
}
